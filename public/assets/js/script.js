document.addEventListener('DOMContentLoaded', () => {
    // Contoh menambahkan class ke elemen yang sudah ada
    document.getElementById('tambah').classList.add('js-example-basic-single');

    // Tambahkan elemen ke DOM
    document.body.appendChild(newElement);
});
